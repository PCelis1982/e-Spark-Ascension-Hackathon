# Current Stack Snapshot (2025-11-10)

- Backend (e-spark-ccs): Firestore/Postgres; calc endpoints; sessions + intensity model
- Frontend (e-spark-app): projects page; calc screens
- Guardian: pending (disk space); plan is local Docker
- Hedera: testnet account to be created (see below)
